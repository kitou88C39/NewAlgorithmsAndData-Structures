import java.util.Scanner;

public class Main {

    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        int l = sc.nextInt(), n = sc.nextInt(), k = sc.nextInt();

        int[] a = new int[k]; // 必要に応じて変更する
        for (int i = 0; i < k; i++) {
            a[i] = sc.nextInt();
        }

        // ここにコードを書く
    }
}
